select * from product;

select * from authentication_details;

CREATE TABLE authentication_details
(
	auth_detail_id INT IDENTITY(1,1) NOT NULL PRIMARY KEY,
	product_id INT,
	auth_type NVARCHAR(50),
	one_to_one_max_user INT,
	one_to_many_max_user INT,
	one_to_one_max_template INT,
	one_to_many_max_template INT
)

INSERT INTO authentication_details VALUES ()