1. 출입통제기 상세  내역 - 조인으로 테이블 불러올시 중복 데이터 문제




2. request.ParseMultipartForm(8 << 20)
request.ParseMultipartForm(1 << 30)

메모리 할당시 비트 연산자 사용
-> 표기시 편리해서 사용?





3. modifyDevice.clx
그리드에서 수정값 전달시 데이터 셋 문제
기존에 값을 가져와 세팅한 데이터 셋에서 수정된 값을 서버로
전달이 되는데 수정되지 않은 값이나 그리드 체크박스에서 체크 해제한 값들은 전달되지 않음 (인증방식)

그리드에서 제거한 열도 변경된 열로 취급되어 수정된 열과 같이 값이 전달 (담당 개발자)

-> delete값을 전달한 데이터 셋을 따로 만들어 거기에 값을 담아 전달

다른 방식이 있는지??




4. 커스터마이징 이력
버전별로 그리드 따로 그리기...? -> 직접 코드로 작성해서 그려주는 방식으로 해야하는지?





5. writeOutput.clx에서 여러 File 객체 전달시 setFileParameters 사용해서
request.MultipartForm으로 받았는데 multipartForm.Value시 File 객체 하나만 꺼내져서 현재는 for문으로 addFileParameter를 사용해 File 객체를 하나씩 따로 담아준 뒤에 request.MultipartForm으로 꺼냄
한번에 여러 File 객체를 전달해서 받는 방법은 없는지?




6. udc....



